# Moderation Architecture

The moderation contract has moved to the focused documentation set in [`docs/moderation/`](moderation/README.md).

Start with:

- [Moderation Documentation](moderation/README.md);
- [Moderation Core Architecture](moderation/architecture.md);
- [Rule Catalog Contract](moderation/rule-catalog-contract.md).

This file is a compatibility pointer and is not a second source of truth.
